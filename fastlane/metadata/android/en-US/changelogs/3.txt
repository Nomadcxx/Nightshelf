Removes Google Cast, the last proprietary dependency. NightShelf is now
entirely free software, and 1.7 MB smaller. The cost: no Chromecast.
Android Auto is unaffected.

Targets Android 16 (API 36) and lays out edge to edge under it.
Adds the F-Droid metadata this listing is built from.
Nothing else about playback, sync or downloads changes.
